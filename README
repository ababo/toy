Simple OS-like program for x86-64 which dreams to become a real OS.

Started to work on version 0.2 from scratch in order to avoid a painful
refactoring. This version will support multiboot specification and SMP from the
ground up.

The previous version can be found in branch 'ver.0.1'.